#!/usr/bin/env bash
# Placeholder for Dr Stone custom menu (Omarchy inspired)
# Can use rofi for initial testing, replacing with AGS later if possible.

rofi -show drun -theme-str "window {width: 400px; border-radius: 10px; border: 2px solid #a855f7;}"
